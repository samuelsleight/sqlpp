#ifndef SQLLIB_RESULTS_HPP
#define SQLLIB_RESULTS_HPP

#include "defines.hpp"

SQLLIB_NS
/*
template<typename, typename... RowTypes>
class Select;

template<typename RowTuple, typename... RowTypes>
class SelectResult {
public:
    bool next();

private:
    friend class Select<RowTuple, RowTypes...>;

    DB* db;
    RowTuple rows;

    SelectResult(DB* db, RowTuple rows);
};
*/
SQLLIB_NS_END

#endif
